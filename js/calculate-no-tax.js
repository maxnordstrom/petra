jQuery(document).ready(function() {


  // Set variable to number of table rows
  let $numberOfRows = document.getElementsByClassName("table-row").length;

  // Calculate no tax
  let noTax = $(
    `<td>Hej</td>`
  );



  $('#product-row').append(noTax);

});